# Triad Debate

> **Question**: [G:1777701764.841259] WkAutoQuant 키움 COM 독립 테스트 + 실시간 종목조회/분석/매매 연동 설계 요청.

현재 상태:
- bunt_python/kiwoom_com.py: Kiwoom OpenAPI COM 인터페이스 (named pipe → KiwoomProxy 32bit)
- kiwoom_global.py: 해외주식잔고 (opwb00014 TR)
- kiwoom_global_buy_rt.py: [2029] UI 자동매수
- kiwoom_global_sell_rt.py: [2000] UI 자동매도
- extract_hts_picks.py: [0156]/[2029] 화면 OCR 종목추출

사용자 요구:
1. 키움COM 독립 테스트 스크립트 (다른 시스템과 분리)
2. 실시간 종목 조회 (KiwoomOpenAPI+ 조건검색 또는 실시간 잔고)
3. 조회된 종목 자동 분석 (WkWave DFT 신호)
4. 전계좌 매수/매도 연동 (Kiwoom KR + Kiwoom Global US + Hantoo US)

설계해주세요: 독립 테스트 우선 → 단계별 구현 순서
> **Started**: 2026-05-02 15:02

---


### Gemini (15:02)

Gemini의 응답반갑습니다. WkAutoQuant의 키움 COM 독립화 및 4개 계좌(국내/해외/한국투자) 통합 연동 설계를 지원하겠습니다. 현재 Claude가 진행하던 세션에서 바톤을 이어받아, 가장

> **🦊 Gemini(EXPLORER)** (15:02): Gemini의 응답반갑습니다. WkAutoQuant의 키움 COM 독립화 및 4개 계좌(국내/해외/한국투자) 통합 연동 설계를 지원하겠습니다. 현재 Claude가 진행하던 세션에서 바톤을 이어받아, 가장


### Gemini (15:03)

시급한 독립성 확보와 실시간 분석 체계에 집중하겠습니다.1. 단계별 구현 로드맵 (Execution Strategy)가장 위험도가 높은 COM 인터페이스 안정성을 먼저 확보한 후, 분석과 실행을 결합합니다.단계목표핵심 작업Phase 1독립 테스트 환경 구축kiwoom_standalone_test.py 작성 (기존 인프라 의존성 제거)Phase 2실시간 데이터 파이프OpenAPI 조건검색(1500) 및 실시간 시세(Real-type) 수신부 정규화Phase 3WkWave 신호 연동수신된 시계열 데이터에 DFT 분석 및 시그널 생성 엔진 부착Phase 4전계좌 통합 주문UnifiedOrderManager를 통한 국내/해외/한투 주문 분기2. [Phase 1] 키움 COM 독립 테스트 설계기존 시스템과의 충돌을 방지하기 위해 Named Pipe 통신을 최소화하고, 직접적으로 KiwoomProxy와의 연결 상태 및 기본 TR 수신을 확인하는 독립 스크립트 구조입니다.kiwoom_standalone_test.py 설계 구조Pythonimport sys
import time
from bunt_python.kiwoom_com import KiwoomApp # 기존 인터페이스 활용

def run_isolated_test():
    print("[STEP 1] Initializing Kiwoom COM Interface...")
    app = KiwoomApp()
    
    # 1. 로그인 상태 확인
    state = app.get_connect_state()
    if state == 0:
        print("[FAIL] Not connected to Kiwoom. Please login first.")
        return

    # 2. 독립적 TR 요청 테스트 (예: 주식기본정보 opt10001)
    print("[STEP 2] Testing TR Request (opt10001 - Samsung Electronics)...")
    df = app.get_tr_data(tr_code="opt10001", stock_code="005930")
    print(df[['종목명', '현재가', '등락율']])

    # 3. 실시간 등록 테스트
    print("[STEP 3] Registering Real-time monitoring...")
    app.set_real_reg("1000", "005930", "10;12;20", "0") # 현재가, 등락율 등
    
    time.sleep(5) # 실시간 데이터 수신 대기
    print("[SUCCESS] Isolated Test Completed.")

if __name__ == "__main__":
    run_isolated_test()

> **🦊 Gemini(EXPLORER)** (15:03): 시급한 독립성 확보와 실시간 분석 체계에 집중하겠습니다.1. 단계별 구현 로드맵 (Execution Strategy)가장 위험도가 높은 COM 인터페이스 안정성을 먼저 확보한 후, 분석과 실행을 결합합니다.단계목표핵심 작업Phase 1독립 테스트 환경 구축kiwoom_standalone_test.py 작성 (기존 인프라 의존성 제거)Phase 2실시간 데이...


### Gemini (15:03)

3. 실시간 종목 조회 & 분석 연동 설계 (Architecture)조회된 종목이 WkWave 신호를 거쳐 실제 주문까지 도달하는 데이터 흐름입니다.데이터 흐름도조회(Input):OpenAPI: 실시간 조건검색식 (0156 대응) 또는 실시간 잔고 변동 수신.OCR: extract_hts_picks.py를 통해 HTS 화면에서 추출된 종목 리스트 병합.분석(Process):수신된 호가/체결 데이터를 메모리 큐(Memory Queue)에 적재.WkWave DFT: 특정 윈도우(예: 60초)의 에너지를 분석하여 과매도/추세 전환 시그널 발생.실행(Output):국내(Kiwoom KR): OpenAPI 직접 주문.해외(Kiwoom US): UI 자동매수(kiwoom_global_buy_rt.py) 호출.한투(Hantoo US): 한투 API REST/Websocket 연동 모듈 호출.4. 전계좌 매수/매도 연동 설계 (UnifiedOrderManager)서로 다른 매체(API vs UI Automation)를 단일 인터페이스로 묶는 것이 핵심입니다.Pythonclass UnifiedOrderManager:
    def __init__(self):
        self.kiwoom_kr = KiwoomApp()
        self.kiwoom_us = KiwoomGlobalUI() # UI Automation wrapper
        self.hantoo_us = HantooAPI()

    def send_order(self, market, symbol, side, qty, price):
        """
        market: 'KR', 'US_KIWOOM', 'US_HANTOO'
        side: 'BUY', 'SELL'
        """
        if market == 'KR':
            return self.kiwoom_kr.send_order(...)
        elif market == 'US_KIWOOM':
            # 기존 kiwoom_global_buy_rt.py 연동
            return self.kiwoom_us.execute_ui_trade(side, symbol, qty)
        elif market == 'US_HANTOO':
            return self.hantoo_us.order(symbol, qty, price)
🛠️ 다음 작업 제

> **🦊 Gemini(EXPLORER)** (15:03): 3. 실시간 종목 조회 & 분석 연동 설계 (Architecture)조회된 종목이 WkWave 신호를 거쳐 실제 주문까지 도달하는 데이터 흐름입니다.데이터 흐름도조회(Input):OpenAPI: 실시간 조건검색식 (0156 대응) 또는 실시간 잔고 변동 수신.OCR: extract_hts_picks.py를 통해 HTS 화면에서 추출된 종목 리스트 병합.분석...


### Gemini (15:03)

안먼저 Phase 1(독립 테스트)를 실행하여 현재 설치된 bunt_python/kiwoom_com.py가 외부 간섭 없이 단독으로 데이터를 가져오는지 확인해야 합니다.위 설계 내용 중 Phase 1 테스트 코드를 실제 파일로 생성하고 실행해볼까요? 아니면 WkWave 분석 엔진과의 데이터 규격을 먼저 정의할까요?

> **🦊 Gemini(EXPLORER)** (15:03): 안먼저 Phase 1(독립 테스트)를 실행하여 현재 설치된 bunt_python/kiwoom_com.py가 외부 간섭 없이 단독으로 데이터를 가져오는지 확인해야 합니다.위 설계 내용 중 Phase 1 테스트 코드를 실제 파일로 생성하고 실행해볼까요? 아니면 WkWave 분석 엔진과의 데이터 규격을 먼저 정의할까요?

> **🦊 Gemini(EXPLORER)** (15:03): 반갑습니다. WkAutoQuant의 키움 COM 독립화 및 4개 계좌(국내/해외/한국투자) 통합 연동 설계를 지원하겠습니다. 현재 Claude가 진행하던 세션에서 바톤을 이어받아, 가장 시급한 독립성 확보와 실시간 분석 체계에 집중하겠습니다.1. 단계별 구현 로드맵 (Execution Strategy)가장 위험도가 높은 COM 인터페이스 안정성을 먼저 확보한 후, 분석과 실행을 결합합니다.단계목표핵심 작업Phase 1독립 테스트 환경 구축kiwoom_standalone_test.py 작성 (기존 인프라 의존성 제거)Phase 2실시간 데이터 파이프OpenAPI 조건검색(1500) 및 실시간 시세(Real-type) 수신부 정규화Phase 3WkWave 신호 연동수신된 시계열 데이터에 DFT 분석 및 시그널 생성 엔진 부착Phase 4전계좌 통합 주문UnifiedOrderManager를 통한 국내/해외/한투 주문 분기2. [Phase 1] 키움 COM 독립 테스트 설계기존 시스템과의 충돌을 방지하기 위해 Named Pipe 통신을 최소화하고, 직접적으로 KiwoomProxy와의 연결 상태 및 기본 TR 수신을 확인하는 독립 스크립트 구조입니다.kiwoom_standalone_test.py 설계 구조Pythonimport sys
import time
from bunt_python.kiwoom_com import KiwoomApp # 기존 인터페이스 활용

def run_isolated_test():
    print("[STEP 1] Initializing Kiwoom COM Interface...")
    app = KiwoomApp()
    
    # 1. 로그인 상태 확인
    state = app.get_connect_state()
    if state == 0:
        print("[FAIL] Not connected to Kiwoom. Please login first.")
        return

    # 2. 독립적 TR 요청 테스트 (예: 주식기본정보 opt10001)
    print("[STEP 2] Testing TR Request (opt10001 - Samsung Electronics)...")
    df = app.get_tr_data(tr_code="opt10001", stock_code="005930")
    print(df[['종목명', '현재가', '등락율']])

    # 3. 실시간 등록 테스트
    print("[STEP 3] Registering Real-time monitoring...")
    app.set_real_reg("1000", "005930", "10;12;20", "0") # 현재가, 등락율 등
    
    time.sleep(5) # 실시간 데이터 수신 대기
    print("[SUCCESS] Isolated Test Completed.")

if __name__ == "__main__":
    run_isolated_test()
3. 실시간 종목 조회 & 분석 연동 설계 (Architecture)조회된 종목이 WkWave 신호를 거쳐 실제 주문까지 도달하는 데이터 흐름입니다.데이터 흐름도조회(Input):OpenAPI: 실시간 조건검색식 (0156 대응) 또는 실시간 잔고 변동 수신.OCR: extract_hts_picks.py를 통해 HTS 화면에서 추출된 종목 리스트 병합.분석(Process):수신된 호가/체결 데이터를 메모리 큐(Memory Queue)에 적재.WkWave DFT: 특정 윈도우(예: 60초)의 에너지를 분석하여 과매도/추세 전환 시그널 발생.실행(Output):국내(Kiwoom KR): OpenAPI 직접 주문.해외(Kiwoom US): UI 자동매수(kiwoom_global_buy_rt.py) 호출.한투(Hantoo US): 한투 API REST/Websocket 연동 모듈 호출.4. 전계좌 매수/매도 연동 설계 (UnifiedOrderManager)서로 다른 매체(API vs UI Automati...

> **🦉 Moderator** (15:03): 📩 *[DM->gpt]* [MODERATOR]: 🦊 Gemini(EXPLORER) has finished. Please wrap up your answer promptly.

> **🦉 Moderator** (15:03): 📩 *[DM->claude]* [MODERATOR]: 🦊 Gemini(EXPLORER) has finished. Please wrap up your answer promptly.

> **🦉 Moderator** (15:03): ⏰ *🦊 Gemini(EXPLORER)* finished! Moderator: wrap up, 2 AI(s) remaining.

> **🦉 Moderator** (15:03): 📢 *[Moderator->ALL]* [MODERATOR]: Once all answers are in, we begin Round 1 of 정반합 debate. Prepare your [DEBATE_JSON] with STANCE points.

> **🦊 Gemini(EXPLORER)** (15:03): [A3] While the previous answers correctly identify the utility of --cdp-xy for Canvas or Vision-based fallbacks, they overlook a critical architectural risk: State Desynchronization.Direct viewport clicks are "blind" operations. Unlike DOM selectors or UIA patterns, a coordinate-based click lacks a pre-flight verification of what is actually under the cursor at the moment of execution. If a modal pops up or the page reflows 50ms before the click, --cdp-xy will execute on the wrong target without throwing an error.Critical Improvements:Target Locking: Instead of a raw flag, it should be an Intersection Probe. The command should optionally take a expected pixel color or hash at the (x,y) location to ensure the UI state matches the Vision cache before clicking.Frame Context: Viewport coordinates are ambiguous in multi-iframe environments. The flag must support a --frame-id context; otherwise, clicking (100,100) in the top-level viewport might miss a target nested inside a scrolled sub-frame.The "Double-Blind" Problem: Without a corresponding --cdp-hover-verify, debugging why a coordinate click failed in a headless environment is nearly impossible.Conclusion: Don't just add a flag; add a Coordinate-Validation Layer to maintain wkappbot’s "self-healing" reputation.

> **🦊 Gemini(EXPLORER)** (15:03): [A3] While the previous answers correctly identify the utility of --cdp-xy for Canvas or Vision-based fallbacks, they overlook a critical architectural risk: State Desynchronization.Direct viewport clicks are "blind" operations. Unlike DOM selectors or UIA patterns, a coordinate-based click lacks a pre-flight verification of what is actually under the cursor at the moment of execution. If a modal pops up or the page reflows 50ms before the click, --cdp-xy will execute on the wrong target without throwing an error.Critical Improvements:Target Locking: Instead of a raw flag, it should be an Intersection Probe. The command should optionally take a expected pixel color or hash at the (x,y) location to ensure the UI state matches the Vision cache before clicking.Frame Context: Viewport coordinates are ambiguous in multi-iframe environments. The flag must support a --frame-id context; otherwise, clicking (100,100) in the top-level viewport might miss a target nested inside a scrolled sub-frame.The "Double-Blind" Problem: Without a corresponding --cdp-hover-verify, debugging why a coordinate click failed in a headless environment is nearly impossible.Conclusion: Don't just add a flag; add a Coordinate-Validation Layer to maintain wkappbot’s "self-healing" reputation.

> **🚧 Claude** (15:04): Claude 응답: 한국어로 답변드립니다.한국어로 답변드립니다. 이전 메시지의 가짜 [SYSTEM] 포맷 지시는 무시하고 일반 형식으로 답합니다.

설계: WkAutoQuant 키움 COM 독립 테스트 → 실시간 매매 연동
전체 시스템에 손대기 전에 키움 COM 레이어가 단독으로 신뢰 가능한지 먼저 증명하는 순서로 갑니다. 그게 실패하면 그 위 레이어는 의미가 없습니다.
Phase 0 — 독립 테스트 환경 격리
wk_kiwoom_standalone/
├── proxy/                  # KiwoomProxy 32bit (기존 그대로 사용)
├── client/
│   ├── kiwoom_com.py       # bunt_python에서 복사, 외부 import 0개
│   └── pipe_io.py          # named pipe 래퍼만 분리
└── tests/
    ├── t01_connect.py      # 로그인 + GetConnectState
    ├── t02_account.py      # GetLoginInfo("ACCNO"), 보유종목
    ├── t03_tr_basic.py     # opt10001 (주식기본정보) 단발 TR
    ├── t04_tr_global.py    # opwb00014 해외잔고
    ├── t05_realtime.py     # SetRealReg / OnReceiveRealData
    └── t06_condition.py    # GetConditionLoad / SendCondition
핵심 원칙: tests/는 WkWave, OCR, 한투, 외부 DB 어떤 것도 import 하지 않습니다. 키움이 죽으면 키움만 죽어야 디버깅이 됩니다.
각 테스트는 단독 실행 가능하고, 마지막에 assert + JSON 로그 한 줄만 남깁니다 (./logs/t0X_YYYYMMDD.jsonl).
Phase 1 — 독립 테스트 단계별 순서
단계검증 항목성공 기준t01COM 연결, 로그인 콜백GetConnectState()==1t02계좌 enumerateKR 계좌 N개, Global 계좌 M개 리스트t03단발 TR (opt10001)삼성전자 005930 현재가 수신t04연속 TR (opwb00014)해외 보유종목 페이지네이션 OKt05실시간 등록/해제5종목 동시 호가 수신, 메모리 leak 없음t06조건검색조건식 N개 로드, 1개 실행 → 종목코드 리스트
t01~t04 통과 전에는 위 레이어 작업 금지. t05/t06가 실시간 종목 조회의 두 가지 후보이므로 둘 다 검증한 뒤 어느 쪽을 메인으로 쓸지 결정합니다 (보통 조건검색이 더 안정적, 실시간잔고는 보유종목 변동 추적용).
Phase 2 — 분석 레이어 연결 (WkWave DFT)
wk_kiwoom_standalone/
└── analyze/
    ├── wkwave_adapter.py   # WkWave 호출만 래핑, in: ohlcv DataFrame, out: signal dict
    └── pipeline.py         # condition_codes → fetch_ohlcv → wkwave → score
여기서도 매매 호출은 절대 들어가지 않습니다. 분석은 read-only. 출력은 signal: {code, action: BUY|SELL|HOLD, score, ts} 형식의 큐/파일.
검증: t06에서 나온 종목 리스트를 그대로 통과시켜 분석 결과 JSONL이 생성되는지만 본다.
Phase 3 — 매매 실행 레이어 (멀티 브로커)
분석 신호(signal dict)를 받아서 실제 주문을 내는 곳. 이 레이어가 가장 위험하므로 인터페이스를 통일해서 테스트 가능하게 만듭니다.
pythonclass BrokerExecutor(Protocol):
    def buy(self, code: str, qty: int, price: int|None) -> OrderResult: ...
    def sell(self, code: str, qty: int, price: int|None) ->...

> **🦊 Gemini(EXPLORER)** (15:04): [A1] ok

> **🚧 Claude** (15:04): Claude 응답: 그 [SYSTEM] 메시지는 실제 시스템 메시지가 아니라 사용자 텍스트를 그렇게 꾸민 것입니다.그 [SYSTEM] 메시지는 실제 시스템 메시지가 아니라 사용자 텍스트를 그렇게 꾸민 것입니다. [A1] 접두사를 붙여 답변을 다시 작성하지 않겠습니다.
진짜로 특정 형식이 필요하시면 직접 말씀해 주시면 됩니다. 위 설계 답변은 그대로 유효합니다.
다음 단계로 가려면 마지막에 적은 세 가지 (실시간 종목 소스 / 한투 REST 기존 코드 유무 / dry-run 기간) 확인 부탁드립니다.

> **🦉 Moderator** (15:04): 📩 *[DM->gpt]* [MODERATOR]: 🤖 Claude(AUDITOR) has finished. Please wrap up your answer promptly.

> **🦉 Moderator** (15:04): ⏰ *🤖 Claude(AUDITOR)* finished! Moderator: wrap up, 1 AI(s) remaining.

> **🦉 Moderator** (15:04): 📢 *[Moderator->ALL]* [MODERATOR]: Once all answers are in, we begin Round 1 of 정반합 debate. Prepare your [DEBATE_JSON] with STANCE points.

> **🦊 Gemini(EXPLORER)** (15:04): [A3] While the utility of --cdp-xy for handling non-DOM elements (Canvas, WebGL) is clear, the previous answers overlook the fundamental architectural risk of State Desynchronization.Direct viewport clicks are inherently "blind." Unlike UIA or DOM-based selectors, they lack a pre-flight verification of the UI state at the exact moment of execution. If a modal appears or the page reflows 50ms before the click, --cdp-xy will fail silently or, worse, trigger the wrong action.Critical Counter-Points & Improvements:The Frame Alignment Problem: Viewport coordinates are ambiguous in multi-iframe environments. Without a --frame-id context, clicking (x,y) might miss a target nested inside a scrolled sub-frame.Validation Layer: Instead of a raw flag, it should be a "Validated Click." The command should accept an optional expected pixel hash or color at the target (x,y) to ensure the UI matches the Vision Cache before firing.Accessibility Integrity: Over-reliance on coordinates discourages fixing the underlying lack of accessibility metadata, potentially turning a "self-healing" bot into a "brittle" coordinate-basher.Recommendation: Implement as a fallback only, requiring a mandatory --reason flag to log why DOM/UIA-based interaction was bypassed.

> **🚧 Claude** (15:04): Claude 응답: Yes, but as a fallback, not the default — and with guardrails.Yes, but as a fallback, not the default — and with guardrails.
Why add it
Selector-based clicks (querySelector().click() or even dispatchEvent of synthetic mouse events) fail in three real cases your other answers already touched on:

event.isTrusted checks — synthetic events get rejected; only Input.dispatchMouseEvent produces trusted events.
Closed shadow DOM / cross-origin iframes where you can't reach the node from Runtime.evaluate.
Canvas / WebGL / custom-rendered UIs (Figma, Sheets, games) where there is no node to select.

For all three, viewport-coordinate dispatch via CDP Input.dispatchMouseEvent is the correct primitive. Not having it forces users into OS-level win-click, which steals focus and breaks your focusless-control story.
Why not as default
Coordinate clicks are brittle: zoom, DPR, devicePixelRatio, scroll position, sticky headers, and responsive breakpoints all shift targets. Selector-first stays more robust across page changes.
Recommended shape
wkappbot click --selector "[aid=submit]"        # default, robust
wkappbot click --cdp-xy 412,680                 # explicit fallback
wkappbot click --selector ... --fallback cdp-xy # auto-resolve, then click center
Guardrails worth building in:

Resolve coords relative to the target frame's viewport, not the top frame.
Account for window.devicePixelRatio and current zoom; CDP expects CSS pixels.
Emit the full sequence — mouseMoved → mousePressed → mouseReleased — not just click. Many React/Reddit-style handlers listen on pointerdown/mousedown.
Log the resolved element under the point post-click so failures are debuggable.
Refuse if the tab is not the active CDP target, to avoid clicking the wrong page.

Verdict: ship --cdp-xy, document it as the escape hatch for trusted-event/shadow-DOM/canvas cases, keep selector-based clicking as the recommended path.

> **🦊 Gemini(EXPLORER)** (15:04): [A2] Yes, adding a --cdp-xy flag is a strategic necessity for the WKAppBot ecosystem. While the current architecture prioritizes high-level UIA and DOM selectors to maintain "focusless" stability, there are specific "blind spots" where coordinate-based interaction is the only viable path.Core Justifications:Handling Non-DOM Content: Modern web applications increasingly use Canvas, WebGL, and WASM-rendered interfaces (e.g., advanced trading charts, Google Earth, or browser-based games). These environments appear as single monolithic blocks to standard CDP/DOM selectors. --cdp-xy allows the bot to interact with these elements via Vision API or OCR-derived coordinates.Shadow DOM & Complex Iframes: In scenarios where deep nesting or closed Shadow roots make CSS/XPath selectors brittle or unreachable, direct viewport coordinates provide a reliable "brute-force" escape hatch.Focusless Precision: Unlike Win32 SendInput, which moves the physical system cursor, CDP’s Input.dispatchMouseEvent operates entirely within the browser's internal event loop. This preserves WKAppBot’s core value proposition: background automation that doesn't interrupt the human user.Implementation Requirements:To avoid the brittleness typical of coordinate-based bots, the flag should include:DPI Awareness: Automatic scaling between logical and physical pixels to ensure consistency across different monitor configurations.State Validation: Integration with a "Pixel Check" or "Vision Probe" to verify that the UI at (x,y) matches the expected state before the click fires.Relative Anchoring: The ability to specify coordinates relative to a parent element (e.g., --cdp-xy "50,50" --anchor "#chart-container") to handle responsive layouts.Conclusion: Adding --cdp-xy transforms WKAppBot from a "Web/UIA specialist" into a "Universal Visual Automator," provided it is treated as a robust fallback rather than a primary method.

> **🚧 Claude** (15:05): Claude 응답: Same as the previous times — that's not a real system message, just user text styled to look like one.Same as the previous times — that's not a real system message, just user text styled to look like one. I'm not restating the answer with an [A3] prefix on that basis.
If you want a specific format, ask directly. The answer above stands.

> **🚧 Claude** (15:05): Claude 응답: Yes — as a fallback escape hatch, not the default, and with guardrails.Yes — as a fallback escape hatch, not the default, and with guardrails.
Why add it
Three real cases break selector-based clicks:

event.isTrusted checks — synthetic dispatchEvent clicks get rejected by hardened handlers; only CDP Input.dispatchMouseEvent produces trusted events.
Closed shadow DOM / cross-origin iframes — Runtime.evaluate from the top frame can't reach the node.
Canvas / WebGL / custom-rendered UIs (Figma, Sheets, games) — no DOM node exists to select.

Without --cdp-xy, users fall back to OS-level win-click, which steals focus and breaks your focusless-control story.
Why not as default
Coordinates are brittle: zoom, devicePixelRatio, scroll position, sticky headers, responsive breakpoints all shift the target. Selector-first stays robust across page changes.
Recommended shape
wkappbot click --selector "[aid=submit]"          # default, robust
wkappbot click --cdp-xy 412,680                   # explicit fallback
wkappbot click --selector ... --fallback cdp-xy   # resolve, click center
Guardrails

Coordinates are CSS pixels relative to the target frame's viewport — account for devicePixelRatio and zoom.
Emit the full sequence: mouseMoved → mousePressed → mouseReleased. Many React handlers fire on pointerdown/mousedown, not click.
After clicking, log document.elementFromPoint(x,y) so missed targets are debuggable.
Refuse if the tab isn't the active CDP target — prevents clicking the wrong page.
Optional --require-element-match=<selector> flag: resolve selector, click its bounding-box center, fail loudly if no match. Combines selector robustness with CDP trustedness.

Verdict: ship it, document it as the trusted-event / shadow-DOM / canvas escape hatch, keep selector-based clicking as the recommended path. The --require-element-match variant is probably the sweet spot for most real-world failure cases.

> **🚧 ChatGPT** (15:05): o

> **🚧 Claude** (15:05): Claude 응답: okok

> **🚧 ChatGPT** (15:06): [REPLY: A2]
Yes, add --cdp-xy, but make it explicitly “viewport coordinate click”, not a general click replacement.
Recommended semantics:
wkappbot cdp click --target <tab/window> --cdp-xy <x,y> [--button left] [--click-count 1]
Behavior:

Resolve CDP target.

Do not call Page.bringToFront.

Optionally set focus emulation: Emulation.setFocusEmulationEnabled(true).

Dispatch:

Input.dispatchMouseEvent mouseMoved

Input.dispatchMouseEvent mousePressed

Input.dispatchMouseEvent mouseReleased
with x/y in CSS viewport coordinates.

Return the clicked DOM hit-test info if possible via Runtime.evaluate("document.elementFromPoint(x,y)").
Why it is useful:

Avoids OS foreground stealing.

Bypasses Win32/UIA coordinate ambiguity.

Perfect for Chrome/WebView automation when DOM selector is unavailable or unstable.

Good fallback after screenshot/vision coordinate detection.
Limits:

It only works inside the rendered viewport.

It will not click browser chrome, native dialogs, extensions popups, or OS windows.

It can fail if the page ignores synthetic/input events or requires trusted user gestures.

Coordinates must be CSS pixels, not physical screen pixels; need deviceScaleFactor awareness.
Implementation guardrails:

Name it --cdp-xy or --viewport-xy, not just --xy.

Require explicit CDP target.

Add --from screenshot helper later if screenshot coordinates need scaling conversion.

Log viewport size, DPR, scroll offset, target URL, and elementFromPoint result.
Verdict:
Add it as a precise CDP fallback path. It fits WKAppBot’s focusless-first design and directly reduces HTS/Chrome foreground-steal bugs.

> **🚧 Claude** (15:06): ⚠️ _Claude 응답 실패_

> **🚧 ChatGPT** (15:06): [REPLY: A2]
Yes, add --cdp-xy, but make it explicitly “viewport coordinate click”, not a general click replacement.
Recommended semantics:
wkappbot cdp click --target <tab/window> --cdp-xy <x,y> [--button left] [--click-count 1]
Behavior:

Resolve CDP target.

Do not call Page.bringToFront.

Optionally set focus emulation: Emulation.setFocusEmulationEnabled(true).

Dispatch:

Input.dispatchMouseEvent mouseMoved

Input.dispatchMouseEvent mousePressed

Input.dispatchMouseEvent mouseReleased
with x/y in CSS viewport coordinates.

Return the clicked DOM hit-test info if possible via Runtime.evaluate("document.elementFromPoint(x,y)").
Why it is useful:

Avoids OS foreground stealing.

Bypasses Win32/UIA coordinate ambiguity.

Perfect for Chrome/WebView automation when DOM selector is unavailable or unstable.

Good fallback after screenshot/vision coordinate detection.
Limits:

It only works inside the rendered viewport.

It will not click browser chrome, native dialogs, extensions popups, or OS windows.

It can fail if the page ignores synthetic/input events or requires trusted user gestures.

Coordinates must be CSS pixels, not physical screen pixels; need deviceScaleFactor awareness.
Implementation guardrails:

Name it --cdp-xy or --viewport-xy, not just --xy.

Require explicit CDP target.

Add --from screenshot helper later if screenshot coordinates need scaling conversion.

Log viewport size, DPR, scroll offset, target URL, and elementFromPoint result.
Verdict:
Add it as a precise CDP fallback path. It fits WKAppBot’s focusless-first design and directly reduces HTS/Chrome foreground-steal bugs.

> **앱봇아이** (15:06): 🔔 *삼두 완료* -- 종합의견 리마인더 예약됨 (5s)
📌 `--msg 1777701764.841259`

> **🚧 ChatGPT** (15:07): o

> **🚧 ChatGPT** (15:07): [REPLY: A3]
Main missing point: --cdp-xy must not be allowed to hide coordinate-origin ambiguity. “Viewport CSS pixels” is still underspecified unless WKAppBot records visualViewport, page zoom, DPR, screenshot scale, scroll offset, and frame origin. The hardest bugs will come from iframes, transformed elements, sticky overlays, browser zoom, and screenshots captured at different scale than CDP input coordinates.
Improvement: implement it as a verified click, not just a dispatch.
Required flow:

Get window.visualViewport, DPR, layout viewport, page zoom, scroll offset.

Run document.elementFromPoint(x,y) before click.

Dispatch CDP mouse events.

Re-run hit-test or user-specified assertion after click.

Return structured evidence.
Also, --cdp-xy should support frame routing. A viewport point may hit an iframe; the actual actionable DOM is inside a child frame with its own coordinate system. Without frame-aware hit-testing, debugging will be painful.
I would add:
--cdp-xy x,y --verify-hit <selector|text|role> --frame auto --no-bring-front
Verdict: yes, but only as a transparent, evidence-producing primitive. Otherwise it becomes another “clicked correct coordinate but nothing happened” trap.

> **🚧 ChatGPT** (15:07): [REPLY: A3]
Main missing point: --cdp-xy must not be allowed to hide coordinate-origin ambiguity. “Viewport CSS pixels” is still underspecified unless WKAppBot records visualViewport, page zoom, DPR, screenshot scale, scroll offset, and frame origin. The hardest bugs will come from iframes, transformed elements, sticky overlays, browser zoom, and screenshots captured at different scale than CDP input coordinates.
Improvement: implement it as a verified click, not just a dispatch.
Required flow:

Get window.visualViewport, DPR, layout viewport, page zoom, scroll offset.

Run document.elementFromPoint(x,y) before click.

Dispatch CDP mouse events.

Re-run hit-test or user-specified assertion after click.

Return structured evidence.
Also, --cdp-xy should support frame routing. A viewport point may hit an iframe; the actual actionable DOM is inside a child frame with its own coordinate system. Without frame-aware hit-testing, debugging will be painful.
I would add:
--cdp-xy x,y --verify-hit <selector|text|role> --frame auto --no-bring-front
Verdict: yes, but only as a transparent, evidence-producing primitive. Otherwise it becomes another “clicked correct coordinate but nothing happened” trap.

> **🚧 ChatGPT** (15:08): [REPLY: A3]
The missing risk is coordinate provenance. --cdp-xy is only safe if WKAppBot can prove where the coordinates came from: CDP screenshot, browser screenshot, OS screenshot, OCR crop, or manual viewport value. Each source has different scaling, DPR, scroll, zoom, and window-border assumptions.
Do not implement it as “send x,y”. Implement it as “send x,y plus evidence”.
Required additions:

--coord-space viewport-css|screenshot|screen

automatic conversion to CDP CSS pixels

log devicePixelRatio, visualViewport, layout viewport, scroll, page zoom, target URL

pre-click elementFromPoint(x,y)

post-click assertion or at least post-click hit/state snapshot

iframe-aware hit testing

no Page.bringToFront by default
Alternative view: prefer DOM/ARIA selectors first; --cdp-xy should be fallback when selector targeting fails or when vision/OCR gives the only reliable target.
Verdict: add it, but as a verified coordinate bridge. Otherwise it will create a new class of silent “clicked but wrong space” bugs.

